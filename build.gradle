// 루트 프로젝트: 공통 설정만 담당. 실제 Spring Boot 앱은 각 서브프로젝트(user, topic, quiz, interview, ranking-streak, gateway)에 있습니다.
subprojects {
    group = 'com.deadlock'
    version = '0.0.1-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}
